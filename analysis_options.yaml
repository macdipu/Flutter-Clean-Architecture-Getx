analyzer:
  exclude:
    - build/**
    - build/**/SourcePackages/**
    - .dart_tool/**
    - ios/Pods/**
    - macos/Flutter/ephemeral/**
    - .idea/**

# Keep default lints for now. Add project-specific lints later if desired.
